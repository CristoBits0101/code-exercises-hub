public class POO {
    
    // La palabra clave "super" se usa para acceder a los constructores del padre y agregar nuevos parámetros.
    
    // Override permite que un método tenga el mismo nombre pero diferentes parámetros o contenido.
    
    // Abstración y Interfaces hacen lo mismo la diferencia son los casos de uso:

    // Las clases abstractas se acceden por herencia para evitar acceso a una clase en construcción con variables en común para diferentes objetos.

    // Las interfaces se utilizan para reutilizar código y establecer un modelo común de desarrollo con variables en común para diferentes objetos.

}